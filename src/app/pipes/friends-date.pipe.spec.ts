import { FriendsDatePipe } from './friends-date.pipe';

describe('FriendsDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FriendsDatePipe();
    expect(pipe).toBeTruthy();
  });
});
